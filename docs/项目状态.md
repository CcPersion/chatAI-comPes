# ⚠️ DEPRECATED — v1 遗留状态文档
# v2 当前状态见 workflow-state.md (2026-08-05 已完成全部 5 步)
# 此文档仅供参考。

# 本地真人 AI 赛博女友｜长任务状态 (v1 弃用)

## 项目元数据

- 状态：执行中
- 当前阶段：阶段 1B-lite，轻量本地真人视觉层与语音闭环
- 负责人/主控窗口：当前 Codex 总控会话
- 权威方案：`docs/项目方案.md`
- 工作流：`D:\AI\Persie-bin\98-context\长任务项目协作工作流.md`
- 最后更新：2026-08-01

## 最终目标

先交付一个本机 Windows Web 端本地 AI 语音伴侣，核心模型、ASR、TTS、后端与轻量写实视觉层均在本地运行；3D Unreal/MetaHuman 作为后续升级目标。

## 不可改变的约束

- 不调用云端推理接口。
- 不用模拟数据或云端推理接口代替真实验收。
- `1B-lite` 可使用写实图片/轻量 talking head，但不得宣称已完成 Unreal/3D 数字人验收。
- 页面不展示调试面板、情绪面板或模型参数面板。

## 验收标准

- [ ] 阶段 1A：浏览器页面可访问，文字输入后本机 Ollama 流式回复，停止/错误提示基本可用。
- [ ] 阶段 1B-lite：浏览器显示写实女性视觉层，并能随待机/倾听/思考/说话状态变化。
- [ ] 阶段 2：SenseVoice/Whisper 语音输入、CosyVoice/Qwen3-TTS 语音输出和停止打断可用。
- [ ] 阶段 3：轻量 talking head 口型、表情、动作与音频联动，通过三轮人工验收。
- [ ] 后续升级：Unreal/MetaHuman/Pixel Streaming 3D 真人数字人单独验收。

## 已确认结论

- 当前目录初始只有需求文档和参考图。
- Node.js 24.13.0、npm 11.6.2、Python 3.14.2 可用。
- `ollama` CLI 不在 PATH，但 `http://127.0.0.1:11434/api/tags` 可访问。
- 本机 Ollama 已有 `qwen2.5:7b-instruct`，7.6B，Q4_K_M。
- 已确认 Unreal Engine 5.8.1 已安装在 `E:\AI-code\UE_5.8`，编辑器路径为 `E:\AI-code\UE_5.8\Engine\Binaries\Win64\UnrealEditor.exe`。
- 用户确认先降级路线：不再让当前阶段阻塞于 MetaHuman/Unreal，先按截图同类的本地 speech-to-speech + 轻量写实视觉层跑通全流程。
- 独立审查结论：阶段 1 初稿不能算完整通过，只能算 1A 候选初稿；1B 阻塞于真实 Unreal/MetaHuman/Pixel Streaming 资产和启动方式。
- 独立验证结论：`npm.cmd run check` 通过，`npm.cmd test` 3/3 通过，后端和前端可启动，`/api/health` 和 `/api/chat` 本机 Ollama SSE 通过。
- 独立验证发现：Ollama 回复中曾出现可见 `neutral` 标签，违反“情绪标签内部使用”要求，已派实现子会话返工。
- 返工实现结论：已新增可见情绪标签清理，前端状态文案改为“Ollama 文本服务已连接；Unreal / Pixel Streaming 未验收”，`npm.cmd run check` 与 `npm.cmd test` 5/5 通过。
- 返工复验结论：第一次独立复验未完成 `/api/health`、`/api/chat` SSE 和前端文案验证；3100/5173 端口已清理，无残留监听。
- 第二次返工复验结论：仍未完成真实 HTTP/SSE 和前端文案复验；曾发现 3100 残留 node 监听 PID 80672，已由复验子会话停止，最终 3100/5173 无残留监听。

## 当前阶段

### 阶段目标

- 创建可运行源码、目录与文档。
- 实现 React 前端和 Node 本地协调服务。
- 真实调用本机 Ollama 流式生成文本。
- 提供 Pixel Streaming 嵌入入口和不可用时的明确提示。

### 本阶段禁止事项

- 不把 Pixel Streaming iframe 占位当成真人数字人验收通过。
- 不把 ASR/TTS 适配器未启用状态包装成语音阶段完成。
- 不新增云端推理依赖。

## 风险与偏离

- 风险：Unreal/MetaHuman 工程资产和 Pixel Streaming 服务尚未在本目录提供。
- 风险：SenseVoice 与 CosyVoice 本地服务尚未安装或启动。
- 风险：阶段 1 只能验证 Web/后端/Ollama；不能完成最终真人感验收。
- 偏离点：初始化阶段由主控直接落地了阶段 1 初稿实现，未先按知识库流程派实现/审查/验证子会话分工。
- 修正措施：主控停止继续扩展实现，将现有代码视为阶段 1 初稿；已派独立审查子会话和独立验证子会话，后续由主控根据审查/验证结果做集成门禁。
- 偏离点：页面“本地服务已连接”文案可能误导为 Pixel Streaming/Unreal 也已连接。
- 修正措施：已派阶段 1A 返工实现子会话，仅修正情绪标签泄露和状态文案，不推进阶段 2。

## 下一步

1. 建立 `1B-lite` 视觉组件：写实女性图像、待机/倾听/思考/说话状态。
2. 选择本地 ASR/TTS 最小可运行组合。
3. 跑通本地 speech-to-speech：VAD/ASR -> Ollama -> TTS -> 页面播放与视觉状态。

## 门禁记录

### 2026-08-01｜初始化门禁

- 总目标是否仍一致：是
- 当前阶段是否正确：是，阶段 1
- 是否出现未经授权的新方向：有，主控承担了本应下放给子会话的实现工作
- 是否读取了权威资料：是
- 当前产出是否通过验收：待独立审查和验证
- 纠偏动作：把未提供的 Unreal/SenseVoice/CosyVoice 明确标记为未验收；已派子会话执行审查和验证，主控回到集成门禁角色。

### 2026-08-01｜阶段 1A/1B 拆分门禁

- 总目标是否仍一致：是
- 当前阶段是否正确：是，阶段 1A 返工；阶段 1B 待真实 Unreal 输入
- 是否出现未经授权的新方向：无
- 是否读取了权威资料：是
- 当前产出是否通过验收：1A 自动验证部分通过但需返工；1B 未通过
- 纠偏动作：不进入阶段 2；先修 1A 标签泄露和状态误导，再等待用户提供 Unreal/MetaHuman 工程资产或 Pixel Streaming 启动方案。

### 2026-08-01｜阶段 1A 返工复验门禁

- 总目标是否仍一致：是
- 当前阶段是否正确：是，阶段 1A 复验
- 是否出现未经授权的新方向：无
- 是否读取了权威资料：是
- 当前产出是否通过验收：未通过完整复验
- 纠偏动作：第一次复验只确认 `check` 和 `test` 通过，未完成真实 HTTP/SSE/前端文案复验；需要重派更窄复验。

### 2026-08-01｜阶段 1A 第二次复验门禁

- 总目标是否仍一致：是
- 当前阶段是否正确：是，阶段 1A 复验
- 是否出现未经授权的新方向：无
- 是否读取了权威资料：是
- 当前产出是否通过验收：未通过真实 HTTP/SSE 复验
- 纠偏动作：第二次复验仍只确认 `check` 和 `test` 通过，未拿到 `/api/health`、`/api/chat` SSE 和前端文案证据；不得标记 1A 通过真实复验。

## 接力摘要

- 当前状态：阶段 1A 已通过真实 HTTP/SSE 复验；阶段 1B 进入环境准备但未具备验收条件。
- 已完成：需求、Obsidian 工作流、环境初查、阶段 1A 文本闭环、1A 确定性复验脚本、1B 环境盘点脚本、1B 操作清单。
- 当前只做：安装/准备 Unreal、真人资产和 Pixel Streaming 链路；不进入阶段 2。
- 不要做：不要宣称真人数字人、ASR、TTS 已验收。
- 需要用户确认：Unreal 安装路径、`.uproject` 路径、真人资产来源和 Pixel Streaming 启动方式。

### 2026-08-01 - 阶段 1A 确定性复验门禁

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1A 复验。
- 是否出现未经授权的新方向：无；未推进 ASR/TTS/Unreal。
- 当前产出是否通过验收：阶段 1A 真实 HTTP/SSE 复验通过；阶段 1B 仍未验收。
- 复验证据：`npm.cmd run check` 通过；`npm.cmd test` 通过，5/5；`npm.cmd run verify:1a` 通过。
- `verify:1a` 范围：Node 子进程启动 `apps/orchestrator/src/server.mjs`，等待 `/api/health`，请求 `/api/chat` SSE 到 `done`，断言用户可见 `token`、`sentence.text`、`done.text` 不泄露 `neutral/happy/caring/serious/surprised/shy` 或 `情绪：`，静态断言前端 `text-ready` 文案。
- 清理结果：脚本结束后 `port.3100.listening=false`，未启动 Vite，未发现 3100 残留监听。
- 门禁结论：1A 可标记为“Web/后端/Ollama 文本闭环通过真实复验”；阶段 1 完整验收仍等待 1B Unreal Pixel Streaming 真人待机画面。

### 2026-08-01 - 阶段 1B 环境准备门禁

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1B 环境准备。
- 是否出现未经授权的新方向：无；未安装、下载或创建假 Unreal 工程。
- 当前产出是否通过验收：1B 未通过，当前只是项目内准备层完成。
- 准备证据：`npm.cmd run check` 通过；`npm.cmd test` 通过，5/5；`npm.cmd run check:1b-env` 通过但结论为 `ready=false / not-ready-for-1b`。
- 盘点结果：已发现 Unreal Engine 5.8.1 和引擎内 Pixel Streaming / Pixel Streaming2 插件；未发现 `.uproject`、SignallingWebServer、MetaHuman/Quixel/Fab/Bridge 资产线索；80、8888、3100、5173 均空闲。
- 门禁结论：无需再安装 Unreal；下一步必须创建真实 `.uproject`、准备真实写实女性资产，并补齐 Pixel Streaming Signalling/Web Server 链路，才能继续 1B 验收。

### 2026-08-02 - 阶段 1B Unreal 安装识别修正

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1B 环境准备。
- 是否出现未经授权的新方向：无。
- 当前产出是否通过验收：1B 未通过，Unreal 安装条件已满足。
- 修正原因：Epic Launcher 显示 Unreal Engine 5.8.1 已安装；原 `check:1b-env` 未读取 Epic manifest 和 `E:\AI-code` 实际安装路径。
- 修正证据：`npm.cmd run check` 通过；`npm.cmd run check:1b-env` 已识别 `E:\AI-code\UE_5.8\Engine\Binaries\Win64\UnrealEditor.exe`，并发现引擎内 Pixel Streaming / Pixel Streaming2 插件目录。
- 剩余缺口：没有 `.uproject`，没有 MetaHuman/真人资产线索，没有 SignallingWebServer 启动链路。

### 2026-08-02 - 阶段 1B Unreal 项目创建确认

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1B 环境准备。
- 是否出现未经授权的新方向：无。
- 当前产出是否通过验收：1B 未通过，Unreal 项目条件已满足。
- 复核证据：`npm.cmd run check:1b-env` 已识别 `D:\codexWorkSpec\chatAI-comPes\unreal\CyberCompanion_1B\CyberCompanion_1B.uproject`。
- 当前已具备：Unreal Engine 5.8.1、真实 `.uproject`、引擎内 Pixel Streaming / Pixel Streaming2 插件线索、80/8888/3100/5173 空闲。
- 剩余缺口：没有 MetaHuman/真人资产线索，没有 Pixel Streaming SignallingWebServer 启动链路，尚未在浏览器看到实时真人待机画面。

### 2026-08-01 - 阶段 1B 环境准备门禁

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1A 已通过真实复验；阶段 1B 进入 Unreal / Pixel Streaming 环境与资产准备。
- 是否出现未经授权的新方向：无；本次只补环境盘点脚本、1B 操作清单和状态记录，不安装 Unreal / Epic / 资产，不创建假 Unreal 工程，不改 Web / ASR / TTS 业务功能。
- 当前产出是否通过验收：1B 未通过；当前只具备环境准备层。
- 新增准备项：`scripts/check-1b-env.mjs` 和 `npm.cmd run check:1b-env`，用于盘点 UnrealEditor.exe、`.uproject`、Pixel Streaming Infrastructure / SignallingWebServer、MetaHuman / Quixel / Fab / Bridge 线索以及 80 / 8888 / 3100 / 5173 端口占用。
- 文档补充：`docs/1B-Unreal-PixelStreaming-操作清单.md` 明确从无环境到 1B 验收的手动步骤、禁止伪造项和验收证据。
- 门禁结论：1A 可作为文本闭环通过；1B 需要真实 Unreal 工程、真实写实真人资产和真实 Pixel Streaming 浏览器画面后才能验收。

### 2026-08-02 - 技术路线降级门禁

- 总目标是否仍一致：调整为先交付本地 AI 语音伴侣完整闭环，后续再升级 3D 真人数字人。
- 当前阶段是否正确：是，进入阶段 1B-lite。
- 是否出现未经授权的新方向：无，用户明确要求先降级。
- 当前产出是否通过验收：文档和验收口径已调整，功能实现待开始。
- 决策：Unreal/MetaHuman/Pixel Streaming 不再作为当前阶段阻塞项；保留为后续升级路线。
- 下一步：先实现轻量写实视觉层，再接本地 ASR/TTS 和 talking head。

### 2026-08-02 - 阶段 1B-lite 前端视觉层实现门禁

- 总目标是否仍一致：是，先跑通本地 AI 伴侣完整闭环，后续再升级 3D 真人数字人。
- 当前阶段是否正确：是，阶段 1B-lite 前端写实视觉层。
- 是否出现未经授权的新方向：无；本次只替换当前主视觉，不接 ASR/TTS/Unreal，不下载资源，不启动服务。
- 当前产出是否通过验收：代码级门禁已通过；真实浏览器视觉验收待主控后续安排。
- 实现范围：`src/main.jsx` 移除 Pixel Streaming iframe，改为使用本地 `3D真人数字人效果图.png` 的 1B-lite 写实视觉舞台；`src/styles.css` 增加待机、倾听、思考、说话和异常状态下的呼吸、状态光、柔和波形。
- 边界声明：当前视觉资产只是 1B-lite 占位，不伪造口型、表情识别、真人视频或 3D/Unreal 验收。
- 验证项：`scripts/verify-1a.mjs` 已更新前端静态断言；新增 `scripts/verify-1b-lite.mjs` 和 `npm.cmd run verify:1b-lite`，确保当前主视觉不包含 `<iframe`、不依赖 `config.pixelStreamingUrl`，也不把主视觉标记为 `Unreal Pixel Streaming`。

### 2026-08-02 - 阶段 1B-lite 独立复验门禁

- 总目标是否仍一致：是。
- 当前阶段是否正确：是，阶段 1B-lite 前端视觉层复验。
- 是否出现未经授权的新方向：无；未接 ASR/TTS/talking head/3D。
- 当前产出是否通过验收：静态和 HTTP 复验通过；浏览器截图/人眼验收未完成。
- 已验证：`npm.cmd run check` 通过；`npm.cmd test` 通过，5/5；`npm.cmd run verify:1b-lite` 通过；`http://127.0.0.1:5173/`、`/src/main.jsx` 和本地图片资源均返回 200。
- 已确认：当前主视觉源码使用本地 `3D真人数字人效果图.png`，未发现 `<iframe`、`Unreal Pixel Streaming`、`config.pixelStreamingUrl` 作为当前主视觉依赖。
- 未完成：未拿到 Playwright/截图级渲染证据，未完成浏览器 DOM/人眼确认页面只包含主视觉、聊天记录、输入框、麦克风、发送、停止按钮。
- 清理结果：复验子会话曾发现 3100 PID 82604 和 5173 PID 72368 残留，已停止；最终 3100/5173 无监听。
